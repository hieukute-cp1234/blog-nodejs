const authController = require("./auth");
const blogController = require("./blog");
const reactController = require("./react");
const commentController = require("./comment");

module.exports = {
  authController,
  blogController,
  reactController,
  commentController,
};
